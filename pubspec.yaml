name: dashboard_route_app
description: A new Flutter project.
publish_to: "none"
version: 0.1.0

environment:
  sdk: ">=2.19.2 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  latlong2: ^0.8.1
  get: ^4.6.5
  fl_chart: ^0.61.0
  flutter_svg: ^2.0.2
  mongo_dart: ^0.9.1
  percent_indicator: ^4.2.3
  buttons_tabbar: ^1.3.6
  intl: ^0.18.0
  flutter_dotenv: ^5.0.2
  mapbox_gl: ^0.16.0
  flutter_map: ^3.1.0
  universal_html: ^2.2.1
  marquee: ^2.2.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/images/
    - assets/icons/
    - assets/config/
    - assets/icon/
